app.controller("MainCtrl", function($scope, $http, $routeParams, $location, Alert) {

  $scope.Alert = Alert;

  $scope.main = function() {
    /*
    * Entry point of the controller.
    */
  }();
});
