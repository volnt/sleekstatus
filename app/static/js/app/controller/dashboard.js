app.controller("DashboardCtrl", function($scope, $http, $routeParams, $location, Alert) {

  $scope.Alert = Alert;

  $scope.main = function() {
    /*
    * Entry point of the controller.
    */
  }();
});
